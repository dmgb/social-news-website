{% extends 'base.html.twig' %}

{% block body %}
    <div class="inner-container text-center">
        <div class="form-wrapper">
            {{ form_start(form) }}
                {{ form_row(form.plainPassword) }}
                <button class="w-100 btn btn-lg btn-dark">Reset password</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
