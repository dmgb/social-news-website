<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('index') }}">
            <img src="{{ asset('build/images/observation.png') }}" alt="" style="height: 2rem; width: auto;" />
        </a>
        <div style="margin-bottom: 2.25rem;">
            <div class="navbar-center">
                {{ render(controller('App\\Controller\\StoryController::search')) }}
            </div>
        </div>
        <div class="hamburger navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-expanded="false">
            <span class="line"></span>
            <span class="line"></span>
            <span class="line"></span>
        </div>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('story_pending') }}">dashboard</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('story_create') }}">submit</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('comments') }}">comments</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">about</a>
                </li>
            </ul>
            {% if app.user %}
                <div class="me-3 fs-2">
                    <a href="{{ path('messages', { 'folder': 'inbox' }) }}">
                        <img src="{{ asset('build/images/email.png') }}" alt="" style="height: 2rem; width: auto;" />
                        <span class="position-absolute translate-middle rounded-circle"
                              style="margin-top: 2.375rem; padding: .4rem; background: #95d0a9;">
                        </span>
                    </a>
                </div>
            {% endif %}
            <div class="text-end">
                {% if app.user %}
                    <div class="dropdown">
                        <a class="dropdown-toggle"
                           href="#"
                           role="button"
                           id="dropdownMenuLink"
                           data-bs-toggle="dropdown"
                           data-bs-display="static"
                           aria-expanded="false">
                            <img src="{{ asset('build/images/user.png') }}" alt="" style="height: 2rem; width: auto;" />
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-lg-end"
                            aria-labelledby="dropdownMenuLink">
                            <li class="ms-3 d-flex">
                                <div class="d-inline-block me-2">
                                    <img class="avatar-md" src="{{ asset('build' ~ app.user.avatarPath) }}"  alt="" />
                                </div>
                                <div class="d-inline-block">
                                    <b>{{ app.user.userIdentifier }}</b>
                                </div>
                            </li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('user_show', { 'username': app.user.userIdentifier }) }}">
                                    profile
                                    <img src="{{ asset('build/images/user.png') }}" class="avatar-sm position-absolute end-0 mt-1 me-3" alt="" />
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('change-password') }}">
                                    settings
                                    <i class="bi bi-gear-fill position-absolute end-0 me-3"></i>
                                </a>
                            </li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                                <a class="btn btn-dark ms-3 me-3" href="{{ path('logout') }}" role="button"
                                   style="width: 10rem;">
                                    sign out
                                    <i class="bi bi-box-arrow-right ms-1"></i>
                                </a>
                            </li>
                        </ul>
                    </div>
                {% else %}
                    <a class=" btn btn-outline-dark" href="{{ path('login') }}" role="button">
                        log in
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
