{% extends 'base.html.twig' %}
{% block body %}
    <div class="inner-container text-center">
        <div class="form-wrapper">
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_row(form.title) }}
            </div>
            <div class="form-group">
                {{ form_row(form.url) }}
            </div>
            <div class="form-group">
                {{ form_row(form.tags, {'id': 'tom-select'}) }}
            </div>
            <button class="w-100 btn btn-lg btn-dark" type="submit">
                Submit
            </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
